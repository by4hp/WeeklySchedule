{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd server && npm install --omit=dev"
  },
  "deploy": {
    "startCommand": "cd server && cp .env.production .env && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "envVars": {
      "MONGODB_URI": {
        "sync": false
      },
      "PORT": {
        "value": "3001"
      },
      "NODE_ENV": {
        "value": "production"
      }
    }
  }
}
